# Reviewed migrated families

All eighteen migrated families have content review by lychen, as confirmed by
Jiabin. Their activity instructions are unchanged by promotion. This does not
claim timing measurement, child testing or deployment verification.

Move an approved `activity_*.yaml` here from `../_candidates/`; set its reviewer
and status first. Keep one copy of each template ID. The loader never falls back
to drafts. The older category mission files are outside this migration collection.
